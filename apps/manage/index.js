const express = require('express')
const router = express.Router()
const app = require('./app')
// router.post('/recommend-post/:id',app.recommendCreate)
// router.delete('/recommend-post/:_id',app.recommendDelete)


module.exports = router